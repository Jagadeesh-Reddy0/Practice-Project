pipeline {
  agent any
  stages {
    stage('initializing') {
      steps {
        git branch: 'main', credentialsId: 'Jagadeesh', url: 'https://github.com/Jagadeesh-Reddy0/Practice-Project.git'
        echo 'Build'
      }
    }

    stage('build') {
      agent any
      steps {
        echo 'printe'
      }
    }

  }
}
